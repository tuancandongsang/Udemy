import { UtilService, Form, ModalConfirm, ModalNoti, Stethoscope, Medicine } from '../../../../Shared';
import { REGEX_TEL, REGEX_DATE, ONE_DAY, ORDER_TYPE, STATUS } from '../../../../Constances/const';
import { Http } from '../../../../Helper/Http';
import ProductService from '../../../Product/Components/Shared/ProductService';

export {
    REGEX_DATE,
    REGEX_TEL,
    ONE_DAY,
    ORDER_TYPE,
    STATUS,
    UtilService,
    Form as FormParent,
    ModalConfirm,
    ModalNoti,
    Http,
    ProductService,
    Medicine,
    Stethoscope,
}